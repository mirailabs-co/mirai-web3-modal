const pkg = require('../package.json');

exports.helpText = `mirai-scripts v${pkg.version}
  Usage: mirai-scripts <script-name> [options]
  Use e.g. "mirai-scripts build" directly".
  -h --help              Print this help
  -v --version           Print mirai-scripts version number`;

exports.startHelpText = `mirai-scripts v${pkg.version}
  Usage: mirai-scripts start [options]
  Use e.g. "mirai-scripts start" directly".
  -h --help              Print this help
  -n --name              Name of the project`;

exports.buildHelpText = `mirai-scripts v${pkg.version}
  Usage: mirai-scripts build [options]
  Use e.g. "mirai-scripts build" directly".
  -h --help              Print this help
  -n --name              Name of the project`;
